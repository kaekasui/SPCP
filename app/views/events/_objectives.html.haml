- edit ||= false
- collapsed = session[:event_objectives].nil? # && @event.errors.empty?
= subtitle :event_objectives, collapsed, t(:objectives)
.section
  %small#event_objectives_intro{ hidden_if(!collapsed) }
    = t(:intro, t(:objectives_small)) unless edit
  #event_objectives{ hidden_if(collapsed) }
    %small
      #{t :objectives_help}
    %table
      %tr
        %td
          .label #{t :number_of_leads}:
          = f.text_field :target_leads, :style => "width:110px; text-align:right;"
        %td= spacer
        %td
          .label #{t :conversion_label}:
          = f.text_field :target_conversion, :style => "width:110px; text-align:right;"
        %td= spacer
        %td
          .label #{t :revenue_label}:
          = f.text_field :target_revenue, :value => @event.target_revenue ? @event.target_revenue.to_i : "", :style => "width:110px; text-align:right;"
        %td= spacer
        %td
          .label #{t :budget_label}:
          = f.text_field :budget, :value => @event.budget ? @event.budget.to_i : "", :style => "width:110px; text-align:right;"
      %tr
        %td{ :colspan => 7 }
          .label #{t :objectives}:
          = f.text_area :objectives, :style => "width:500px", :rows => 2
