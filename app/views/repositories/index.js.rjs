if @equipment.any?
  page[:equipment].replace_html render(:partial => "equipment", :collection => @equipment)
else
  page[:contacts].replace_html render(:partial => "shared/empty")
end
# page[:paginate].replace_html render(:partial => "shared/paginate")
